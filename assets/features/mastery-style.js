/* Feature package 'mastery-style'. Owned by that package: edit only this file and the inline hot spots
   assigned to it. See index.js for the contract. Nothing runs at import time. */
export const id='mastery-style';
export function install(G){}
