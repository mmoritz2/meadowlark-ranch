/* Feature package 'ranch'. Owned by that package: edit only this file and the inline hot spots
   assigned to it. See index.js for the contract. Nothing runs at import time. */
export const id='ranch';
export function install(G){}
