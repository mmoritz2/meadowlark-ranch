/* Renamed to horse-hair-volume.js when it learned manes too. This stays for one reason: a
   browser that cached the previous breed-models.js (?v=artist-breeds-2) still imports this path,
   and a missing module fails the whole game's module graph, not just the tail. */
export {fillOutTail} from './horse-hair-volume.js';
